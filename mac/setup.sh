sudo echo "";which brew || /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)";mkdir -p ~/atk/temp;which git || brew install git;which rbenv || brew install rbenv;rbenv versions | grep '2.5.5' || rbenv install 2.5.5;touch ~/.bash_profile;touch ~/.zshenv;cat ~/.bash_profile | grep "which rbenv && eval \"\$(rbenv init -)\" # setup for ruby" || echo "which rbenv && eval \"\$(rbenv init -)\" # setup for ruby" >> ~/.bash_profile;cat ~/.zshenv | grep "which rbenv && eval \"\$(rbenv init -)\" # setup for ruby" || echo "which rbenv && eval \"\$(rbenv init -)\" # setup for ruby" >> ~/.zshenv;rbenv init &>/dev/null;rbenv global 2.5.5;rbenv shell 2.5.5;gem install atk_toolbox;curl -fsSL https://raw.githubusercontent.com/aggie-tool-kit/atk/master/setup/setup.rb > ~/atk/temp/setup.rb;ruby ~/atk/temp/setup.rb;rm ~/atk/temp/setup.rb